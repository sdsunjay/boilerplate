<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#"></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <% if user_signed_in? %>
        Logged in as <strong><%= current_user.name %></strong>.
        <li class=" <%= params[:controller] == 'users' ? 'nav-item active' : 'nav-item'%>"><%= link_to 'Account', user_path(current_user), class: "nav-link" %></li>
        <li><%= link_to 'Edit profile', edit_user_registration_path, class: 'nav-link' %></li>
        <li><%= link_to "Logout", destroy_user_session_path, method: :delete, class: 'nav-link'  %></li>
      <% else %>
        <li class="nav-item"><a class="nav-link disabled" href="#">Profile</a></li>
        <li class="nav-item"><a class="nav-link disabled" href="#">Products</a></li>
        <li class="nav-item"><%= link_to "Sign Up", new_user_registration_path %></li>
        <li class="nav-item"><%= link_to "Sign In", new_user_session_path  %></li>
      <% end %>
    </ul>
  </div>
</nav>

